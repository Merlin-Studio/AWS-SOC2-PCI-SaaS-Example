Generated by Merlin Studio (https://app.merlin-studio.cloud). Licensed under the Apache License, Version 2.0 (https://www.apache.org/licenses/LICENSE-2.0).

# Input → Output Assertion Findings

These items in the wizard spec are not represented in the rendered
output. Each is a divergence between the user's intent and the deployed
Landing Zone.

- **Format divergence** `Central log bucket Object Lock (LZA)` — Frameworks pci_dss, soc2 require WORM (S3 Object Lock) retention on the central log archive. The OpenTofu/CDK formats emit it natively; the LZA centralLogBucket configuration has no object-lock setting in the supported schema version, so it must be applied out of band (enable Object Lock at bucket creation and add a COMPLIANCE-mode default retention).
- **Unsupported section** `Application Services (15_application_services)` — Section is enabled with content (ecs_clusters) but is NOT emitted in any AWS output format (LZA / OpenTofu / CDK / tfvars). Provision these resources out of band or via a custom module — the wizard does not yet generate them.
