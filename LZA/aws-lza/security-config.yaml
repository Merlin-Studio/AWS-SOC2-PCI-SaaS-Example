# Generated by Merlin Studio (https://app.merlin-studio.cloud). Licensed under the Apache License, Version 2.0 (https://www.apache.org/licenses/LICENSE-2.0).

# -----------------------------------------------------------------------------
# Generated by Merlin Studio v2 (architex-v2) — LZA security-config.yaml
# Organization: AcmeCorp
# Profile:      STANDARD
# Generated:    2026-08-04T14:55:49.150630Z
# Wizard:       v1.0.0
# -----------------------------------------------------------------------------
# Scaffolding — not a finished artifact. Merlin renders this from your
# declared intent (profile, frameworks, accounts, topology). Adapt it to
# your environment: tighten CIDRs, refine policy bodies, add team tags,
# pin module versions, layer account-specific overrides. Re-run the wizard
# when *intent* shifts; keep day-to-day customisation in your IaC fork
# (regen overwrites this directory).
# Replace PLACEHOLDER_ tokens with real values from your AWS Organization
# before `tofu apply` / `cdk deploy` / LZA pipeline run — see PLACEHOLDERS.md.
# Compliance frameworks declared on this spec:
#   - cis
#   - pci_dss
#   - iso27001
#   - soc2

# Centralised security services: GuardDuty, Security Hub, Config, Macie,
# Access Analyzer, IAM password policies, AWS Config rules.
# Schema: https://awslabs.github.io/landing-zone-accelerator-on-aws/v1.14/typedocs/

centralSecurityServices:
  delegatedAdminAccount: Audit

  ebsDefaultVolumeEncryption:
    enable: true
    # ⚠️ encryption_requirements=cmk wants the workload CMK (alias/workload/default),
    # but LZA's `kmsKey` references a key NAME defined in this config's
    # keyManagementService.keySets — not an alias. Define that CMK in keySets,
    # then set `kmsKey: <its-name>` here. Until then EBS uses the AWS-managed key.
    excludeRegions: []

  s3PublicAccessBlock:
    enable: true
    excludeAccounts: []

  scpRevertChangesConfig:
    enable: true

  snsSubscriptions:
    - level: High
      email: "security@acme.com"
    - level: Medium
      email: "security@acme.com"
    - level: Low
      email: "security@acme.com"

  macie:
    enable: true
    excludeRegions: []
    policyFindingsPublishingFrequency: FIFTEEN_MINUTES
    publishSensitiveDataFindings: true
    lifecycleRules:
      - enabled: true
        expiration: 365
        noncurrentVersionExpiration: 90
        abortIncompleteMultipartUpload: 7

  # ⚠️ Amazon Inspector is REQUIRED for this landing zone (inspector_enabled=true;
  # PCI-DSS 11.3.1 / NIST RA-5) but the Landing Zone Accelerator has no native
  # Inspector block under centralSecurityServices. ENABLE IT OUT-OF-BAND: register
  # the audit account as the Inspector delegated admin and turn on org auto-enable
  # for ECR/EC2/Lambda. The OpenTofu, CDK and tfvars formats render this natively.
  #   aws inspector2 enable-delegated-admin-account --delegated-admin-account-id <AUDIT>
  #   aws inspector2 update-organization-configuration --auto-enable ec2=true,ecr=true,lambda=true

  guardduty:
    enable: true
    excludeRegions: []
    s3Protection:
      enable: true
      excludeRegions: []
    eksProtection:
      enable: true
      excludeRegions: []
    # GuardDuty Malware Protection (LZA v1.14 ec2Protection):
    # forensic scanning of EBS volumes on workloads flagged in findings.
    # HIPAA §164.308(a)(6)(ii) for PHI workloads. keepSnapshots retains
    # snapshots of infected volumes as forensic evidence.
    ec2Protection:
      enable: true
      keepSnapshots: true
      excludeRegions: []
    # RDS Protection — on for regulated frameworks or when RDS/Aurora is in use
    # (bank transactional data concentrates there); gated by the GuardDuty SoT.
    rdsProtection:
      enable: true
      excludeRegions: []
    autoEnableOrgMembers: true
    exportConfiguration:
      enable: true
      destinationType: S3
      exportFrequency: FIFTEEN_MINUTES
    lifecycleRules:
      - enabled: true
        expiration: 365
        noncurrentVersionExpiration: 90
        abortIncompleteMultipartUpload: 7

  auditManager:
    enable: false
    excludeRegions: []
    defaultReportsConfiguration:
      enable: true
      destinationType: S3
    lifecycleRules:
      - enabled: true
        expiration: 2555
        noncurrentVersionExpiration: 90
        abortIncompleteMultipartUpload: 7

  detective:
    enable: false
    excludeRegions: []

  securityHub:
    enable: true
    regionAggregation: true
    excludeRegions: []
    standards:
      - name: aws-foundational-security-best-practices/v/1.0.0
        enable: true
        controlsToDisable: []
      - name: cis-aws-foundations-benchmark/v/3.0.0
        enable: true
        controlsToDisable: []
      - name: pci-dss/v/4.0.1
        enable: true
        controlsToDisable: []

  ssmAutomation:
    excludeRegions: []
    documentSets:
      - shareTargets:
          organizationalUnits:
            - Root
        documents:
          - name: Attach-IAM-Instance-Profile
            template: ssm-documents/attach-iam-instance-profile.yaml
          - name: Attach-IAM-Role-Policy
            template: ssm-documents/attach-iam-role-policy.yaml

accessAnalyzers:
  enable: true

iamPasswordPolicy:
  allowUsersToChangePassword: true
  hardExpiry: false
  requireUppercaseCharacters: true
  requireLowercaseCharacters: true
  requireSymbols: true
  requireNumbers: true
  minimumPasswordLength: 14
  passwordReusePrevention: 24
  maxPasswordAge: 90

awsConfig:
  enableConfigurationRecorder: true
  enableDeliveryChannel: true
  overrideExisting: false
  aggregation:
    enable: true
    delegatedAdminAccount: Audit
  ruleSets:
    - deploymentTargets:
        organizationalUnits:
          - Root
      rules:
        - name: encrypted-volumes
          identifier: ENCRYPTED_VOLUMES
        - name: rds-storage-encrypted
          identifier: RDS_STORAGE_ENCRYPTED
        - name: s3-bucket-server-side-encryption-enabled
          identifier: S3_BUCKET_SERVER_SIDE_ENCRYPTION_ENABLED
        - name: cloudtrail-enabled
          identifier: CLOUD_TRAIL_ENABLED
        - name: cloud-trail-encryption-enabled
          identifier: CLOUD_TRAIL_ENCRYPTION_ENABLED
        - name: iam-password-policy
          identifier: IAM_PASSWORD_POLICY
        - name: vpc-flow-logs-enabled
          identifier: VPC_FLOW_LOGS_ENABLED
        # IMDSv2 enforcement detection — CIS 5.4 / NIST AC-3. Required by every
        # framework at this tier (FedRAMP Mod, PCI-DSS, NIST 800-53), not just
        # FedRAMP High. Detection only; org-wide deny of IMDSv1 is an SCP concern.
        - name: ec2-imdsv2-check
          identifier: EC2_IMDSV2_CHECK

keyManagementService:
  keySets:
    # NOTE: a compliance framework set CMK rotation to 90 days, but LZA's
    # keySets schema only exposes enableKeyRotation (boolean) -> AWS-managed annual
    # (365d) rotation. The OpenTofu / tfvars / CDK formats honor the 90-day
    # period via rotation_period_in_days. Under LZA, set the shorter period post-deploy
    # (aws kms enable-key-rotation --rotation-period-in-days 90) or manage these
    # CMKs in Terraform to meet the floor.
    - name: workload-default
      alias: alias/workload/default
      description: Default workload CMK
      enableKeyRotation: true
      enabled: true
      deploymentTargets:
        organizationalUnits:
          - Root
