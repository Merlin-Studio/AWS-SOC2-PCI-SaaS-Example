# Generated by Merlin Studio (https://app.merlin-studio.cloud). Licensed under the Apache License, Version 2.0 (https://www.apache.org/licenses/LICENSE-2.0).

# -----------------------------------------------------------------------------
# Generated by Merlin Studio v2 (architex-v2) — LZA network-config.yaml
# Organization: AcmeCorp
# Profile:      STANDARD
# Generated:    2026-08-04T14:55:49.150630Z
# Wizard:       v1.0.0
# -----------------------------------------------------------------------------
# Scaffolding — not a finished artifact. Merlin renders this from your
# declared intent (profile, frameworks, accounts, topology). Adapt it to
# your environment: tighten CIDRs, refine policy bodies, add team tags,
# pin module versions, layer account-specific overrides. Re-run the wizard
# when *intent* shifts; keep day-to-day customisation in your IaC fork
# (regen overwrites this directory).
# Replace PLACEHOLDER_ tokens with real values from your AWS Organization
# before `tofu apply` / `cdk deploy` / LZA pipeline run — see PLACEHOLDERS.md.
# Compliance frameworks declared on this spec:
#   - cis
#   - pci_dss
#   - iso27001
#   - soc2

# VPCs, subnets, Transit Gateway, VPC endpoints, Network Firewall.
# Schema: https://awslabs.github.io/landing-zone-accelerator-on-aws/v1.14/typedocs/

defaultVpc:
  delete: true
  excludeAccounts: []

vpcs:
  - name: hub
    # Per-VPC account from vpc_subnet_plan (hub -> Network, spokes -> workload acct).
    account: Network
    region: us-east-1
    cidrs:
      - 10.0.0.0/16
    enableDnsHostnames: true
    enableDnsSupport: true
    instanceTenancy: default
    # Spoke: no local internet; default route is the TGW (-> inspection).
    internetGateway: false
    vpcFlowLogs:
      trafficType: ALL
      maxAggregationInterval: 600
      destinations:
        - cloud-watch-logs
        - s3
      destinationsConfig:
        cloudWatchLogs:
          retentionInDays: 365
        s3:
          lifecycleRules:
            # VPC flow logs are high-volume operational telemetry — a 1y default
            # avoids a 7y S3 cost drag (the compliance audit archive keeps its
            # own multi-year retention); set s3_retention_days to override.
            - enabled: true
              expiration: 365
              noncurrentVersionExpiration: 90
              abortIncompleteMultipartUpload: 7
      defaultFormat: false
      customFields:
        - version
        - account-id
        - interface-id
        - srcaddr
        - dstaddr
        - srcport
        - dstport
        - protocol
        - packets
        - bytes
        - start
        - end
        - action
        - log-status
        - vpc-id
        - subnet-id
        - instance-id
        - tcp-flags
        - type
        - pkt-srcaddr
        - pkt-dstaddr
    # Spoke route tables: private subnets default to the TGW; isolated stay isolated.
    routeTables:
      - name: hub-tgw-a-rt
        routes: []
      - name: hub-tgw-b-rt
        routes: []
    subnets:
      - name: hub-tgw-a
        availabilityZone: us-east-1a
        ipv4CidrBlock: 10.0.0.0/28
        routeTable: hub-tgw-a-rt
      - name: hub-tgw-b
        availabilityZone: us-east-1b
        ipv4CidrBlock: 10.0.0.16/28
        routeTable: hub-tgw-b-rt
    gatewayEndpoints:
      defaultPolicy: Default
      endpoints:
        - service: s3
        - service: dynamodb
    # Segmented attachment from the topology SoT: associate to the route domain,
    # propagate spoke CIDRs into the inspection domain. Inspection runs appliance
    # mode so the TGW pins each flow to one AZ's firewall endpoint.
    transitGatewayAttachments:
      - name: hub-tgw-attachment
        transitGateway:
          name: main-tgw
          account: Network
        routeTableAssociations:
          - main-tgw-spokes
        routeTablePropagations:
          - main-tgw-inspection
        subnets:
          - hub-tgw-a
          - hub-tgw-b
  - name: workloads
    # Per-VPC account from vpc_subnet_plan (hub -> Network, spokes -> workload acct).
    account: AppProd
    region: us-east-1
    cidrs:
      - 10.1.0.0/16
    enableDnsHostnames: true
    enableDnsSupport: true
    instanceTenancy: default
    # Spoke: no local internet; default route is the TGW (-> inspection).
    internetGateway: false
    vpcFlowLogs:
      trafficType: ALL
      maxAggregationInterval: 600
      destinations:
        - cloud-watch-logs
        - s3
      destinationsConfig:
        cloudWatchLogs:
          retentionInDays: 365
        s3:
          lifecycleRules:
            # VPC flow logs are high-volume operational telemetry — a 1y default
            # avoids a 7y S3 cost drag (the compliance audit archive keeps its
            # own multi-year retention); set s3_retention_days to override.
            - enabled: true
              expiration: 365
              noncurrentVersionExpiration: 90
              abortIncompleteMultipartUpload: 7
      defaultFormat: false
      customFields:
        - version
        - account-id
        - interface-id
        - srcaddr
        - dstaddr
        - srcport
        - dstport
        - protocol
        - packets
        - bytes
        - start
        - end
        - action
        - log-status
        - vpc-id
        - subnet-id
        - instance-id
        - tcp-flags
        - type
        - pkt-srcaddr
        - pkt-dstaddr
    # Spoke route tables: private subnets default to the TGW; isolated stay isolated.
    routeTables:
      - name: wl-private-a-rt
        routes:
          - name: default-to-tgw
            destination: 0.0.0.0/0
            type: transitGateway
            target: main-tgw
      - name: wl-private-b-rt
        routes:
          - name: default-to-tgw
            destination: 0.0.0.0/0
            type: transitGateway
            target: main-tgw
    subnets:
      - name: wl-private-a
        availabilityZone: us-east-1a
        ipv4CidrBlock: 10.1.10.0/24
        routeTable: wl-private-a-rt
        mapPublicIpOnLaunch: false
      - name: wl-private-b
        availabilityZone: us-east-1b
        ipv4CidrBlock: 10.1.11.0/24
        routeTable: wl-private-b-rt
        mapPublicIpOnLaunch: false
    gatewayEndpoints:
      defaultPolicy: Default
      endpoints:
        - service: s3
        - service: dynamodb
    interfaceEndpoints:
      defaultPolicy: Default
      central: false
      allowedCidrs:
        - 10.1.0.0/16
      endpoints:
        - service: ec2
        - service: ssm
        - service: secretsmanager
        - service: logs
    # Segmented attachment from the topology SoT: associate to the route domain,
    # propagate spoke CIDRs into the inspection domain. Inspection runs appliance
    # mode so the TGW pins each flow to one AZ's firewall endpoint.
    transitGatewayAttachments:
      - name: workloads-tgw-attachment
        transitGateway:
          name: main-tgw
          account: Network
        routeTableAssociations:
          - main-tgw-spokes
        routeTablePropagations:
          - main-tgw-inspection
        subnets:
          - wl-private-a
          - wl-private-b
  - name: inspection
    # Per-VPC account from vpc_subnet_plan (hub -> Network, spokes -> workload acct).
    account: Network
    region: us-east-1
    cidrs:
      - 10.5.0.0/16
    enableDnsHostnames: true
    enableDnsSupport: true
    instanceTenancy: default
    # Inspection/egress VPC: hosts the firewall endpoints + per-AZ NAT/IGW.
    internetGateway: true
    vpcFlowLogs:
      trafficType: ALL
      maxAggregationInterval: 600
      destinations:
        - cloud-watch-logs
        - s3
      destinationsConfig:
        cloudWatchLogs:
          retentionInDays: 365
        s3:
          lifecycleRules:
            # VPC flow logs are high-volume operational telemetry — a 1y default
            # avoids a 7y S3 cost drag (the compliance audit archive keeps its
            # own multi-year retention); set s3_retention_days to override.
            - enabled: true
              expiration: 365
              noncurrentVersionExpiration: 90
              abortIncompleteMultipartUpload: 7
      defaultFormat: false
      customFields:
        - version
        - account-id
        - interface-id
        - srcaddr
        - dstaddr
        - srcport
        - dstport
        - protocol
        - packets
        - bytes
        - start
        - end
        - action
        - log-status
        - vpc-id
        - subnet-id
        - instance-id
        - tcp-flags
        - type
        - pkt-srcaddr
        - pkt-dstaddr
    # Per-AZ route tables steer the inspected egress chain. LZA resolves the
    # firewall endpoint per AZ via type: networkFirewall + targetAvailabilityZone.
    routeTables:
      - name: insp-isolated-a-rt
        routes:
          - name: to-firewall
            destination: 0.0.0.0/0
            type: networkFirewall
            target: inspection-nfw
            targetAvailabilityZone: a
      - name: insp-isolated-b-rt
        routes:
          - name: to-firewall
            destination: 0.0.0.0/0
            type: networkFirewall
            target: inspection-nfw
            targetAvailabilityZone: b
      - name: insp-private-a-rt
        routes:
          - name: egress-to-nat
            destination: 0.0.0.0/0
            type: natGateway
            target: insp-public-a-natgw
          - name: return-0
            destination: 10.0.0.0/16
            type: transitGateway
            target: main-tgw
          - name: return-1
            destination: 10.1.0.0/16
            type: transitGateway
            target: main-tgw
          - name: return-2
            destination: 10.2.0.0/16
            type: transitGateway
            target: main-tgw
          - name: return-3
            destination: 10.3.0.0/16
            type: transitGateway
            target: main-tgw
          - name: return-4
            destination: 10.4.0.0/16
            type: transitGateway
            target: main-tgw
          - name: return-5
            destination: 10.6.0.0/16
            type: transitGateway
            target: main-tgw
      - name: insp-private-b-rt
        routes:
          - name: egress-to-nat
            destination: 0.0.0.0/0
            type: natGateway
            target: insp-public-b-natgw
          - name: return-0
            destination: 10.0.0.0/16
            type: transitGateway
            target: main-tgw
          - name: return-1
            destination: 10.1.0.0/16
            type: transitGateway
            target: main-tgw
          - name: return-2
            destination: 10.2.0.0/16
            type: transitGateway
            target: main-tgw
          - name: return-3
            destination: 10.3.0.0/16
            type: transitGateway
            target: main-tgw
          - name: return-4
            destination: 10.4.0.0/16
            type: transitGateway
            target: main-tgw
          - name: return-5
            destination: 10.6.0.0/16
            type: transitGateway
            target: main-tgw
      - name: insp-public-a-rt
        routes:
          - name: to-internet
            destination: 0.0.0.0/0
            type: internetGateway
          - name: return-0
            destination: 10.0.0.0/16
            type: networkFirewall
            target: inspection-nfw
            targetAvailabilityZone: a
          - name: return-1
            destination: 10.1.0.0/16
            type: networkFirewall
            target: inspection-nfw
            targetAvailabilityZone: a
          - name: return-2
            destination: 10.2.0.0/16
            type: networkFirewall
            target: inspection-nfw
            targetAvailabilityZone: a
          - name: return-3
            destination: 10.3.0.0/16
            type: networkFirewall
            target: inspection-nfw
            targetAvailabilityZone: a
          - name: return-4
            destination: 10.4.0.0/16
            type: networkFirewall
            target: inspection-nfw
            targetAvailabilityZone: a
          - name: return-5
            destination: 10.6.0.0/16
            type: networkFirewall
            target: inspection-nfw
            targetAvailabilityZone: a
      - name: insp-public-b-rt
        routes:
          - name: to-internet
            destination: 0.0.0.0/0
            type: internetGateway
          - name: return-0
            destination: 10.0.0.0/16
            type: networkFirewall
            target: inspection-nfw
            targetAvailabilityZone: b
          - name: return-1
            destination: 10.1.0.0/16
            type: networkFirewall
            target: inspection-nfw
            targetAvailabilityZone: b
          - name: return-2
            destination: 10.2.0.0/16
            type: networkFirewall
            target: inspection-nfw
            targetAvailabilityZone: b
          - name: return-3
            destination: 10.3.0.0/16
            type: networkFirewall
            target: inspection-nfw
            targetAvailabilityZone: b
          - name: return-4
            destination: 10.4.0.0/16
            type: networkFirewall
            target: inspection-nfw
            targetAvailabilityZone: b
          - name: return-5
            destination: 10.6.0.0/16
            type: networkFirewall
            target: inspection-nfw
            targetAvailabilityZone: b
    subnets:
      - name: insp-public-a
        availabilityZone: us-east-1a
        ipv4CidrBlock: 10.5.0.0/24
        routeTable: insp-public-a-rt
        mapPublicIpOnLaunch: true
      - name: insp-public-b
        availabilityZone: us-east-1b
        ipv4CidrBlock: 10.5.1.0/24
        routeTable: insp-public-b-rt
        mapPublicIpOnLaunch: true
      - name: insp-private-a
        availabilityZone: us-east-1a
        ipv4CidrBlock: 10.5.10.0/24
        routeTable: insp-private-a-rt
        mapPublicIpOnLaunch: false
      - name: insp-private-b
        availabilityZone: us-east-1b
        ipv4CidrBlock: 10.5.11.0/24
        routeTable: insp-private-b-rt
        mapPublicIpOnLaunch: false
      - name: insp-isolated-a
        availabilityZone: us-east-1a
        ipv4CidrBlock: 10.5.20.0/24
        routeTable: insp-isolated-a-rt
      - name: insp-isolated-b
        availabilityZone: us-east-1b
        ipv4CidrBlock: 10.5.21.0/24
        routeTable: insp-isolated-b-rt
    natGateways:
      - name: insp-public-a-natgw
        subnet: insp-public-a
      - name: insp-public-b-natgw
        subnet: insp-public-b
    gatewayEndpoints:
      defaultPolicy: Default
      endpoints:
        - service: s3
        - service: dynamodb
    # Segmented attachment from the topology SoT: associate to the route domain,
    # propagate spoke CIDRs into the inspection domain. Inspection runs appliance
    # mode so the TGW pins each flow to one AZ's firewall endpoint.
    transitGatewayAttachments:
      - name: inspection-tgw-attachment
        transitGateway:
          name: main-tgw
          account: Network
        options:
          applianceModeSupport: enable
        routeTableAssociations:
          - main-tgw-inspection
        routeTablePropagations: []
        subnets:
          - insp-isolated-a
          - insp-isolated-b
  - name: pci-cde
    # Per-VPC account from vpc_subnet_plan (hub -> Network, spokes -> workload acct).
    account: PciWorkload
    region: us-east-1
    cidrs:
      - 10.2.0.0/16
    enableDnsHostnames: true
    enableDnsSupport: true
    instanceTenancy: default
    # Spoke: no local internet; default route is the TGW (-> inspection).
    internetGateway: false
    vpcFlowLogs:
      trafficType: ALL
      maxAggregationInterval: 600
      destinations:
        - cloud-watch-logs
        - s3
      destinationsConfig:
        cloudWatchLogs:
          retentionInDays: 365
        s3:
          lifecycleRules:
            # VPC flow logs are high-volume operational telemetry — a 1y default
            # avoids a 7y S3 cost drag (the compliance audit archive keeps its
            # own multi-year retention); set s3_retention_days to override.
            - enabled: true
              expiration: 365
              noncurrentVersionExpiration: 90
              abortIncompleteMultipartUpload: 7
      defaultFormat: false
      customFields:
        - version
        - account-id
        - interface-id
        - srcaddr
        - dstaddr
        - srcport
        - dstport
        - protocol
        - packets
        - bytes
        - start
        - end
        - action
        - log-status
        - vpc-id
        - subnet-id
        - instance-id
        - tcp-flags
        - type
        - pkt-srcaddr
        - pkt-dstaddr
    # Spoke route tables: private subnets default to the TGW; isolated stay isolated.
    routeTables:
      - name: pci-private-a-rt
        routes:
          - name: default-to-tgw
            destination: 0.0.0.0/0
            type: transitGateway
            target: main-tgw
      - name: pci-private-b-rt
        routes:
          - name: default-to-tgw
            destination: 0.0.0.0/0
            type: transitGateway
            target: main-tgw
      - name: pci-isolated-a-rt
        routes: []
      - name: pci-isolated-b-rt
        routes: []
    subnets:
      - name: pci-private-a
        availabilityZone: us-east-1a
        ipv4CidrBlock: 10.2.10.0/24
        routeTable: pci-private-a-rt
        mapPublicIpOnLaunch: false
      - name: pci-private-b
        availabilityZone: us-east-1b
        ipv4CidrBlock: 10.2.11.0/24
        routeTable: pci-private-b-rt
        mapPublicIpOnLaunch: false
      - name: pci-isolated-a
        availabilityZone: us-east-1a
        ipv4CidrBlock: 10.2.20.0/24
        routeTable: pci-isolated-a-rt
      - name: pci-isolated-b
        availabilityZone: us-east-1b
        ipv4CidrBlock: 10.2.21.0/24
        routeTable: pci-isolated-b-rt
    gatewayEndpoints:
      defaultPolicy: Default
      endpoints:
        - service: s3
        - service: dynamodb
    interfaceEndpoints:
      defaultPolicy: Default
      central: false
      allowedCidrs:
        - 10.2.0.0/16
      endpoints:
        - service: ec2
        - service: ssm
        - service: secretsmanager
        - service: kms
        - service: logs
        - service: sts
    # Segmented attachment from the topology SoT: associate to the route domain,
    # propagate spoke CIDRs into the inspection domain. Inspection runs appliance
    # mode so the TGW pins each flow to one AZ's firewall endpoint.
    transitGatewayAttachments:
      - name: pci-cde-tgw-attachment
        transitGateway:
          name: main-tgw
          account: Network
        routeTableAssociations:
          - main-tgw-cde
        routeTablePropagations:
          - main-tgw-inspection
        subnets:
          - pci-isolated-a
          - pci-isolated-b
  - name: workloads-dev
    # Per-VPC account from vpc_subnet_plan (hub -> Network, spokes -> workload acct).
    account: AppDev
    region: us-east-1
    cidrs:
      - 10.3.0.0/16
    enableDnsHostnames: true
    enableDnsSupport: true
    instanceTenancy: default
    # Spoke: no local internet; default route is the TGW (-> inspection).
    internetGateway: false
    vpcFlowLogs:
      trafficType: ALL
      maxAggregationInterval: 600
      destinations:
        - cloud-watch-logs
        - s3
      destinationsConfig:
        cloudWatchLogs:
          retentionInDays: 365
        s3:
          lifecycleRules:
            # VPC flow logs are high-volume operational telemetry — a 1y default
            # avoids a 7y S3 cost drag (the compliance audit archive keeps its
            # own multi-year retention); set s3_retention_days to override.
            - enabled: true
              expiration: 365
              noncurrentVersionExpiration: 90
              abortIncompleteMultipartUpload: 7
      defaultFormat: false
      customFields:
        - version
        - account-id
        - interface-id
        - srcaddr
        - dstaddr
        - srcport
        - dstport
        - protocol
        - packets
        - bytes
        - start
        - end
        - action
        - log-status
        - vpc-id
        - subnet-id
        - instance-id
        - tcp-flags
        - type
        - pkt-srcaddr
        - pkt-dstaddr
    # Spoke route tables: private subnets default to the TGW; isolated stay isolated.
    routeTables:
      - name: workloads-dev-private-a-rt
        routes:
          - name: default-to-tgw
            destination: 0.0.0.0/0
            type: transitGateway
            target: main-tgw
      - name: workloads-dev-private-b-rt
        routes:
          - name: default-to-tgw
            destination: 0.0.0.0/0
            type: transitGateway
            target: main-tgw
    subnets:
      - name: workloads-dev-private-a
        availabilityZone: us-east-1a
        ipv4CidrBlock: 10.3.10.0/24
        routeTable: workloads-dev-private-a-rt
        mapPublicIpOnLaunch: false
      - name: workloads-dev-private-b
        availabilityZone: us-east-1b
        ipv4CidrBlock: 10.3.11.0/24
        routeTable: workloads-dev-private-b-rt
        mapPublicIpOnLaunch: false
    gatewayEndpoints:
      defaultPolicy: Default
      endpoints:
        - service: s3
        - service: dynamodb
    # Segmented attachment from the topology SoT: associate to the route domain,
    # propagate spoke CIDRs into the inspection domain. Inspection runs appliance
    # mode so the TGW pins each flow to one AZ's firewall endpoint.
    transitGatewayAttachments:
      - name: workloads-dev-tgw-attachment
        transitGateway:
          name: main-tgw
          account: Network
        routeTableAssociations:
          - main-tgw-spokes
        routeTablePropagations:
          - main-tgw-inspection
        subnets:
          - workloads-dev-private-a
          - workloads-dev-private-b
  - name: workloads-stg
    # Per-VPC account from vpc_subnet_plan (hub -> Network, spokes -> workload acct).
    account: AppStaging
    region: us-east-1
    cidrs:
      - 10.4.0.0/16
    enableDnsHostnames: true
    enableDnsSupport: true
    instanceTenancy: default
    # Spoke: no local internet; default route is the TGW (-> inspection).
    internetGateway: false
    vpcFlowLogs:
      trafficType: ALL
      maxAggregationInterval: 600
      destinations:
        - cloud-watch-logs
        - s3
      destinationsConfig:
        cloudWatchLogs:
          retentionInDays: 365
        s3:
          lifecycleRules:
            # VPC flow logs are high-volume operational telemetry — a 1y default
            # avoids a 7y S3 cost drag (the compliance audit archive keeps its
            # own multi-year retention); set s3_retention_days to override.
            - enabled: true
              expiration: 365
              noncurrentVersionExpiration: 90
              abortIncompleteMultipartUpload: 7
      defaultFormat: false
      customFields:
        - version
        - account-id
        - interface-id
        - srcaddr
        - dstaddr
        - srcport
        - dstport
        - protocol
        - packets
        - bytes
        - start
        - end
        - action
        - log-status
        - vpc-id
        - subnet-id
        - instance-id
        - tcp-flags
        - type
        - pkt-srcaddr
        - pkt-dstaddr
    # Spoke route tables: private subnets default to the TGW; isolated stay isolated.
    routeTables:
      - name: workloads-stg-private-a-rt
        routes:
          - name: default-to-tgw
            destination: 0.0.0.0/0
            type: transitGateway
            target: main-tgw
      - name: workloads-stg-private-b-rt
        routes:
          - name: default-to-tgw
            destination: 0.0.0.0/0
            type: transitGateway
            target: main-tgw
    subnets:
      - name: workloads-stg-private-a
        availabilityZone: us-east-1a
        ipv4CidrBlock: 10.4.10.0/24
        routeTable: workloads-stg-private-a-rt
        mapPublicIpOnLaunch: false
      - name: workloads-stg-private-b
        availabilityZone: us-east-1b
        ipv4CidrBlock: 10.4.11.0/24
        routeTable: workloads-stg-private-b-rt
        mapPublicIpOnLaunch: false
    gatewayEndpoints:
      defaultPolicy: Default
      endpoints:
        - service: s3
        - service: dynamodb
    # Segmented attachment from the topology SoT: associate to the route domain,
    # propagate spoke CIDRs into the inspection domain. Inspection runs appliance
    # mode so the TGW pins each flow to one AZ's firewall endpoint.
    transitGatewayAttachments:
      - name: workloads-stg-tgw-attachment
        transitGateway:
          name: main-tgw
          account: Network
        routeTableAssociations:
          - main-tgw-spokes
        routeTablePropagations:
          - main-tgw-inspection
        subnets:
          - workloads-stg-private-a
          - workloads-stg-private-b
  - name: workloads-sbx
    # Per-VPC account from vpc_subnet_plan (hub -> Network, spokes -> workload acct).
    account: AppSandbox
    region: us-east-1
    cidrs:
      - 10.6.0.0/16
    enableDnsHostnames: true
    enableDnsSupport: true
    instanceTenancy: default
    # Spoke: no local internet; default route is the TGW (-> inspection).
    internetGateway: false
    vpcFlowLogs:
      trafficType: ALL
      maxAggregationInterval: 600
      destinations:
        - cloud-watch-logs
        - s3
      destinationsConfig:
        cloudWatchLogs:
          retentionInDays: 365
        s3:
          lifecycleRules:
            # VPC flow logs are high-volume operational telemetry — a 1y default
            # avoids a 7y S3 cost drag (the compliance audit archive keeps its
            # own multi-year retention); set s3_retention_days to override.
            - enabled: true
              expiration: 365
              noncurrentVersionExpiration: 90
              abortIncompleteMultipartUpload: 7
      defaultFormat: false
      customFields:
        - version
        - account-id
        - interface-id
        - srcaddr
        - dstaddr
        - srcport
        - dstport
        - protocol
        - packets
        - bytes
        - start
        - end
        - action
        - log-status
        - vpc-id
        - subnet-id
        - instance-id
        - tcp-flags
        - type
        - pkt-srcaddr
        - pkt-dstaddr
    # Spoke route tables: private subnets default to the TGW; isolated stay isolated.
    routeTables:
      - name: workloads-sbx-private-a-rt
        routes:
          - name: default-to-tgw
            destination: 0.0.0.0/0
            type: transitGateway
            target: main-tgw
      - name: workloads-sbx-private-b-rt
        routes:
          - name: default-to-tgw
            destination: 0.0.0.0/0
            type: transitGateway
            target: main-tgw
    subnets:
      - name: workloads-sbx-private-a
        availabilityZone: us-east-1a
        ipv4CidrBlock: 10.6.10.0/24
        routeTable: workloads-sbx-private-a-rt
        mapPublicIpOnLaunch: false
      - name: workloads-sbx-private-b
        availabilityZone: us-east-1b
        ipv4CidrBlock: 10.6.11.0/24
        routeTable: workloads-sbx-private-b-rt
        mapPublicIpOnLaunch: false
    gatewayEndpoints:
      defaultPolicy: Default
      endpoints:
        - service: s3
        - service: dynamodb
    # Segmented attachment from the topology SoT: associate to the route domain,
    # propagate spoke CIDRs into the inspection domain. Inspection runs appliance
    # mode so the TGW pins each flow to one AZ's firewall endpoint.
    transitGatewayAttachments:
      - name: workloads-sbx-tgw-attachment
        transitGateway:
          name: main-tgw
          account: Network
        routeTableAssociations:
          - main-tgw-spokes
        routeTablePropagations:
          - main-tgw-inspection
        subnets:
          - workloads-sbx-private-a
          - workloads-sbx-private-b

transitGateways:
  - name: main-tgw
    account: Network
    region: us-east-1
    shareTargets:
      organizationalUnits:
        - Workloads
        - Infrastructure
    asn: 64512
    dnsSupport: enable
    vpnEcmpSupport: enable
    defaultRouteTableAssociation: disable
    defaultRouteTablePropagation: disable
    autoAcceptSharingAttachments: enable
    routeTables:
      - name: main-tgw-inspection
        routes: []
      - name: main-tgw-spokes
        routes:
          - destinationCidrBlock: 0.0.0.0/0
            attachment:
              vpcName: inspection
              account: Network
      - name: main-tgw-cde
        routes:
          - destinationCidrBlock: 0.0.0.0/0
            attachment:
              vpcName: inspection
              account: Network





centralNetworkServices:
  # Centralized inspection (topology SoT). The AWS Network Firewall lives in the
  # inspection VPC; LZA authors stateful rules in firewall-rules/rules.txt (it
  # does not reference AWS-managed rule group ARNs the way the other formats do).
  delegatedAdminAccount: Network
  networkFirewall:
    firewalls:
      - name: inspection-nfw
        firewallPolicy: inspection-nfw-policy
        vpc: inspection
        subnets:
          - insp-private-a
          - insp-private-b
        loggingConfiguration:
          - destination: cloud-watch-logs
            type: ALERT
          - destination: cloud-watch-logs
            type: FLOW
    policies:
      - name: inspection-nfw-policy
        regions:
          - us-east-1
        firewallPolicy:
          statelessDefaultActions:
            - aws:forward_to_sfe
          statelessFragmentDefaultActions:
            - aws:forward_to_sfe
          statefulRuleGroups:
            - name: inspection-nfw-stateful
        shareTargets:
          organizationalUnits:
            - Workloads
    rules:
      - name: inspection-nfw-stateful
        regions:
          - us-east-1
        capacity: 1000
        type: STATEFUL
        ruleGroup:
          rulesSource:
            rulesFile: firewall-rules/rules.txt
        shareTargets:
          organizationalUnits:
            - Workloads

dhcpOptions:
  []

endpointPolicies:
  - name: Default
    document: vpc-endpoint-policies/default.json