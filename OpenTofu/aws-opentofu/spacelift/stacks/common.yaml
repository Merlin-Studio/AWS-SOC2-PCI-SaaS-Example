# Generated by Merlin Studio (https://app.merlin-studio.cloud). Licensed under the Apache License, Version 2.0 (https://www.apache.org/licenses/LICENSE-2.0).

# -----------------------------------------------------------------------------
# Generated by Merlin Studio v2 (architex-v2) — Spacelift common context (shared variables, policies)
# Organization: AcmeCorp
# Profile:      STANDARD
# Generated:    2026-08-04T14:55:24.584722Z
# Wizard:       v1.0.0
# -----------------------------------------------------------------------------
# Scaffolding — not a finished artifact. Merlin renders this from your
# declared intent (profile, frameworks, accounts, topology). Adapt it to
# your environment: tighten CIDRs, refine policy bodies, add team tags,
# pin module versions, layer account-specific overrides. Re-run the wizard
# when *intent* shifts; keep day-to-day customisation in your IaC fork
# (regen overwrites this directory).
# Replace PLACEHOLDER_ tokens with real values from your AWS Organization
# before `tofu apply` / `cdk deploy` / LZA pipeline run — see PLACEHOLDERS.md.

# Shared across every stack. Imported by per-env stacks via the `contexts` field.

aws-default-context:
  name: acmecorp-aws-default-context
  description: Shared variables for all AWS landing-zone stacks.
  labels:
    - aws
    - landing-zone
  environment_variables:
    TF_VAR_organization_name:
      value: AcmeCorp
      write_only: false
    TF_VAR_profile:
      value: standard
      write_only: false
    TF_VAR_cost_center:
      value: PLATFORM
      write_only: false
    TF_VAR_compliance_frameworks:
      value: "cis,pci_dss,iso27001,soc2"
      write_only: false
    SPACELIFT_TOFU_AUTO_REGISTER_PROVIDERS:
      value: "true"
      write_only: false
  mounted_files: []

aws-prod-context:
  name: acmecorp-aws-prod-context
  description: Extra guardrails for production AWS stacks.
  labels:
    - aws
    - landing-zone
    - prod
  environment_variables:
    TF_VAR_environment_type:
      value: prod
      write_only: false
    REQUIRE_APPROVAL:
      value: "true"
      write_only: false
  mounted_files: []