Generated by Merlin Studio (https://app.merlin-studio.cloud). Licensed under the Apache License, Version 2.0 (https://www.apache.org/licenses/LICENSE-2.0).

# Security Scorecard

**Score:** 97/100
**Grade:** A+

Passed: 268
Warned: 1
Failed: 0

**Compliance Frameworks:** CIS, PCI_DSS, ISO27001, SOC2

> **Note:** This is an example landing zone. Rows marked ⚠️ WARN are intentionally left for adopters to configure with their own values (e.g. notification email addresses, DLP info-types relevant to your data). The framework citation on each WARN row still applies — once you substitute real values, those rows will pass.

## Compliance Checks
These checks are performed against your configuration intent based on the selected compliance frameworks.

### Networking (1/1 passed)

| Check | Status | Severity | Frameworks | Reference | Description |
| :--- | :--- | :--- | :--- | :--- | :--- |
| delete_default_vpc | ✅ PASS | recommended | CIS | CIS 5.1 | CIS 5.1 recommends removing the default VPC and restricting default SG |

### Security Baseline (10/10 passed)

| Check | Status | Severity | Frameworks | Reference | Description |
| :--- | :--- | :--- | :--- | :--- | :--- |
| password_min_length | ✅ PASS | required | CIS, PCI_DSS, ISO27001 | CIS 1.8 | CIS 1.8 requires minimum 14-char passwords |
| max_password_age_days | ✅ PASS | required | CIS | CIS 1.10 | CIS 1.10 requires password rotation within 90 days |
| password_reuse_prevention | ✅ PASS | required | CIS, PCI_DSS | CIS 1.9 | CIS 1.9 requires preventing reuse of last 24 passwords |
| cloudtrail_org_enabled | ✅ PASS | required | CIS, PCI_DSS, ISO27001, SOC2 | CIS 3.1 | CIS 3.1 requires CloudTrail enabled in all regions |
| cloudtrail_multi_region | ✅ PASS | required | CIS | CIS 3.1 | CIS 3.1 requires multi-region trail |
| cmek_log_encryption | ✅ PASS | required | CIS, PCI_DSS | CIS 3.7 | CIS 3.7 requires CloudTrail logs encrypted with CMK |
| aws_config_enabled | ✅ PASS | required | CIS, PCI_DSS, ISO27001, SOC2 | CIS 3.5 | CIS 3.5 requires AWS Config enabled in all regions |
| guardduty_enabled | ✅ PASS | required | CIS, PCI_DSS, ISO27001, SOC2 | CIS 4.16 | CIS 4.16 requires GuardDuty enabled |
| security_hub_enabled | ✅ PASS | required | CIS, PCI_DSS, SOC2 | CIS-extended | CIS recommends Security Hub for aggregated findings |
| central_log_retention_days | ✅ PASS | required | PCI_DSS | PCI 10.5.1 | PCI 10.5.1 >=1 year retention |

### Advanced Security (0 passed, 1 warned, 1 total)

| Check | Status | Severity | Frameworks | Reference | Description |
| :--- | :--- | :--- | :--- | :--- | :--- |
| wafv2_logging_enabled | ⚠️ WARN | required | PCI_DSS | PCI 6.4.2 | wafv2_logging_enabled is set, but no WAFv2 web ACL is defined in this landing zone — logging a WAF that does not exist is a no-op. PCI-DSS 6.4.x requires a WAF (or equivalent automated solution) in front of public-facing CDE web apps. Scaffold a web ACL with AWS managed rule groups + logging, and associate it to your public-facing ALB / CloudFront when the workload is defined. |

### Compute Engine (2/2 passed)

| Check | Status | Severity | Frameworks | Reference | Description |
| :--- | :--- | :--- | :--- | :--- | :--- |
| ebs_default_encryption | ✅ PASS | required | CIS, PCI_DSS, SOC2 | CIS 2.2.1 | CIS 2.2.1 — Ensure EBS volume encryption is enabled in all regions |
| imdsv2_required | ✅ PASS | required | CIS | CIS 5.4 | CIS 5.4 — Require IMDSv2 on all EC2 instances (defends against SSRF token theft) |

## Technical Infrastructure Checks (Checkov)
**Passed:** 255 &nbsp;&nbsp; **Failed:** 0

> File paths below reference the canonical **Terraform** (`.tf`) representation that Checkov scans. If your bundle ships a different output format (e.g. tfvars, LZA YAML, or CDK), the `.tf` files are not included — apply the fix to the field of the **same name** in your format's equivalent file, or change it in the wizard and re-generate.

## Suppressed Checks

These Checkov findings are suppressed intentionally — the associated risk is mitigated by the architecture rather than the flagged attribute. The rationale for each is shown below (and inline in the OpenTofu `.tf` source).

| Check | Resource | Rationale |
|-------|----------|-----------|
| `CKV_AWS_274` | `aws_ssoadmin_managed_policy_attachment.admin_aws_managed` — `iam.tf`:L41 | Identity Center cloud-admin / break-glass / CI-apply role; defense-in-depth is org SCPs + CloudTrail alerting, not a boundary on the role itself. |
| `CKV_AWS_274` | `aws_iam_role_policy_attachment.breakglassrole_administratoraccess` — `iam.tf`:L125 | Identity Center cloud-admin / break-glass / CI-apply role; defense-in-depth is org SCPs + CloudTrail alerting, not a boundary on the role itself. |
| `CKV_AWS_274` | `aws_iam_role_policy_attachment.wl_terraformexecutor_administratoraccess` — `iam.tf`:L163 | Identity Center cloud-admin / break-glass / CI-apply role; defense-in-depth is org SCPs + CloudTrail alerting, not a boundary on the role itself. |
| `CKV_AWS_109` | `aws_iam_policy_document.kms_default` — `kms.tf`:L36 | KMS key policy grants the account root kms:* (AWS-prescribed EnableRoot pattern, required for the key to stay manageable); principal scoping bounds the blast radius. |
| `CKV_AWS_111` | `aws_iam_policy_document.kms_default` — `kms.tf`:L36 | Same as CKV_AWS_109 — kms:Update*/Schedule* on the root principal is the documented KMS baseline. |
| `CKV_AWS_356` | `aws_iam_policy_document.kms_default` — `kms.tf`:L36 | Resource="*" is required at the KMS key-policy layer; principal scoping bounds the blast radius. |
| `CKV_AWS_331` | `aws_ec2_transit_gateway.primary` — `transit_gateway.tf`:L33 | — |
| `CKV_TF_1` | `vpc_hub` — `vpc.tf`:L35 | Module is sourced by a version-pinned registry reference, not a mutable git ref. |
| `CKV_TF_1` | `vpc_workloads` — `vpc.tf`:L100 | Module is sourced by a version-pinned registry reference, not a mutable git ref. |
| `CKV_TF_1` | `vpc_pci_cde` — `vpc.tf`:L221 | Module is sourced by a version-pinned registry reference, not a mutable git ref. |
| `CKV_TF_1` | `vpc_workloads_dev` — `vpc.tf`:L362 | Module is sourced by a version-pinned registry reference, not a mutable git ref. |
| `CKV_TF_1` | `vpc_workloads_stg` — `vpc.tf`:L427 | Module is sourced by a version-pinned registry reference, not a mutable git ref. |
| `CKV_TF_1` | `vpc_workloads_sbx` — `vpc.tf`:L492 | Module is sourced by a version-pinned registry reference, not a mutable git ref. |
| `CKV_AWS_144` | `aws_s3_bucket.central_logs` — `s3_log_archive.tf`:L32 | Cross-region replication is opt-in — enable it in the wizard (replication_enabled + destination region) for a full CRR pipeline. Single-region keeps the audit archive in one data-residency boundary, with versioning + Object Lock (WORM) protecting it. |
| `CKV_AWS_18` | `aws_s3_bucket.central_logs` — `s3_log_archive.tf`:L32 | Access logging on the log-archive bucket is circular; CloudTrail S3 data events + SNS notifications cover this surface. |

